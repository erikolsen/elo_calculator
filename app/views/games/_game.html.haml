%tr{id: "game-#{game.id}", align: 'center'}
  %td.winner
    #{game.winner.name} (#{game.winner.rating})
  %td.loser
    #{game.loser.name} (#{game.loser.rating})
  %td.date
    = game.created_at.strftime("%I:%M %p %m/%d")
