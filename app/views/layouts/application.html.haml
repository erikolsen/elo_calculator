!!!
%html
  %head
    %title Ace Ping Pong
    %meta{:content => "width=device-width, initial-scale=1", :name => "viewport"}
    = csrf_meta_tags
    = stylesheet_link_tag 'application'
    = javascript_include_tag 'vendor/modernizr'
  %body
    .contain-to-grid
      %nav.top-bar{"data-topbar" => ""}
        %ul.title-area
          %li.name
            %h1
              = link_to "Ace Ping Pong", root_path
          %li.toggle-topbar.menu-icon
            %a{:href => "#"}
              %span menu
        %section.top-bar-section
          %ul.right
            %li.divider
            %li
              = link_to "Players", players_path
            %li.divider
            %li
              = link_to "Games", games_path
            %li.divider

    .row
      .large-12.columns
        - if flash[:notice]
          .alert-box.success{'data-alert' => nil}
            = flash[:notice]
            %a.close{href: "#"} &times;

        - if flash[:alert]
          .alert-box.alert{'data-alert' => nil}
            = flash[:alert]
            %a.close{href: "#"} &times;

        = yield

    %footer
      .icon-bar.sticky_footer#footer
        = link_to 'Past Games', games_path, class: 'item'
        = link_to 'Add Game', new_game_path, class: 'item'
        =# link_to 'Add Player', new_player_path, class: 'item'
        =# link_to 'Home', root_path, class: 'item'

    = javascript_include_tag 'application'
