%tr{id: "player-#{player.id}", align: 'center'}
  %td.rank
    = player_counter + 1
  %td.name
    = player.name
  %td.rating
    = player.rating
  %td.won-lost
    = "#{player.games_won} / #{player.games_lost}"
